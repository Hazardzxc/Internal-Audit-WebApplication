@model STD.Models.DB.WorkingPaper
@using STD.Models.DB;
@{
    string Title = "กิจกรรมการควบคุมและวิธีการตรวจสอบ";
    ViewData["Title"] = $"เพิ่มข้อมูล{Title}";

    // List<AuditIssueActivity> _ListAuditIssueActivity = new List<AuditIssueActivity>();
    // using (AuditManager_Connect DB = new AuditManager_Connect())
    // {
    //     _ListAuditIssueActivity = DB.AuditIssueActivities.OrderBy(o => o.CreateDate).ToList();
    // }
}